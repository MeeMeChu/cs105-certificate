export type Event = {
  id: string;
  title: string;
  description: string;
  image?: string;
  date: string;
  location: string;
  status: string;
  secretPass: string;
  createdAt?: string;
  updateAt?: string;
}